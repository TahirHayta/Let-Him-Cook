using UnityEngine;

public class HolderCounter : CounterParent
{
    public int holdingDegree; // freezer, or hot storage
    

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
